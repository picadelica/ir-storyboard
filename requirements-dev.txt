# Зависимости для локального прогона тестов.
#   python3 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/python -m pytest -m "not network" -q
#
# Полный рантайм бэкенда (включая тяжёлые ML-пакеты для YouTube-транскрипции)
# живёт в backend/requirements.txt и не нужен для unit/e2e без сети.
-r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.27
pyyaml>=6.0
httpx>=0.27
pytest>=8.0
