# NEXT — где мы остановились

> Снимок оперативного состояния. Overwrite в конце сессии (не журнал). Стабильное — в `CLAUDE.md`.

---

**Последнее обновление:** 2026-07-22
**Ветка:** `feat/v2` (всё запушено, дерево чистое)
**Прод:** 216.57.108.107 (Caddy :80, Basic Auth; `/` health открыт), `deploy_ir_storyboard` зелёный.
Тесты: `pytest -m "not network"` → **314 passed** (сетевой `test_youtube_e2e` deselected).
Локальный dev: backend `uvicorn backend.main:app --port 8080` (DB `data/matrix.db`, gitignored,
это НЕ прод — прод-копия в `data/afisha.db` у другого проекта); фронт — preview `ir-storyboard-dev`
на :5180 (прокси /api→:8080). Прод-БД читать по ssh: `ssh root@216.57.108.107` →
`docker exec ir-storyboard-backend-1 python3 …` над `/app/data/matrix.db` (только чтение!).

## Батч правок карточки/сортировки/фаундеров — ЗАДЕПЛОЕНО НА ПРОД (2026-07-23, deploy COMPLETED)

Коммиты `8747e51`…`c7d5567`, 318 passed. После деплоя прогнали `init_schema` на проде
(колонки sort_order/about_company; см. деплой-нюанс #2). Health 200, матрица работает.
1. **Чистка карточки факта** (`8747e51`): убран флаг-кружок (карточка и так окрашена); спикер —
   компактный чип «🗣 Имя» (клик→правка) вместо постоянного дропдауна; канал источника —
   пиктограмма+название (🌐 веб-ресёрч, 🎧 онлайн-интервью, 🎙 офлайн-интервью, 🗄 архив).
2. **Drag-сортировка карточек** (`2e48a71`): тянуть за ручку ⠿; `facts.sort_order` + reorder-эндпоинт.
3. **Тег «факт про другую компанию»** (`26162c7`): `facts.about_company`, чип «🏢 про: GetTaxi»
   рядом со спикером (кейс Вайзер про GetTaxi).
4. **Кросс-компанийный фаундер** (`c7d5567`): выбран вариант «тот же человек + тег компании».
   `founders_by_name` + `import_founder_profile`; в About у фаундера «👤 тот же в: <компания> ↓» →
   влить профиль (links/role/url/note) в один клик.

**Уже на проде (не через деплой):** @JackyBlack (tid 42022538) добавлен в `IR_ADMIN_TIDS` в
серверном `/opt/ir-storyboard/.env` → суперадмин; backend пересоздан, `is_admin=True`, health 200.
`.env` серверный (не в git), правка переживает деплой.

Открытый хвост фаундеров (по желанию, НЕ делали): факты слоёв 1-2 — сделать спикера обязательным/
заметным нуджем. Пока спикер — необязательный чип.

## Отгружено на прод в эту сессию (2026-07-22)

Все задеплоены (`deploy_ir_storyboard`, health зелёный), паспорт обновлён:

1. **Матрица v2** (`16fc7f3`/`53d8042`) — новые имена+описания подсекций (`~/Downloads/matrix new.md`).
   L6/L8: 6.1 «Architecture & Philosophy» (слито с бывш. Philosophy), 6.2 «Market context»,
   6.3 «Product & company evolution», 8.1 «Social», 8.2 «Technology», 8.3 «Political & Economical».
   Миграция `matrix_v2_names_desc_v3` (one-time, guard app_meta) применяется при рестарте app. Коды не тронуты.
2. **Переклассификация фактов** (`3a3d9c4`) — «Применить новую методологию» (per-client): async-джоб
   пересчитывает раскладку active-фактов текущей методологией → превью переездов с чекбоксами и
   **редактируемой целью** (override → amber «вручную») → apply. `llm.reclassify_facts`,
   `matrix.active_facts_for_reclassify`/`move_fact_to_subsection`.
3. **Ручной перенос факта** (`773bd7c`) — `POST /api/facts/{id}/move`; в CellDrawer селект «Раздел:»
   (24 подсекции). Текст иммутабелен (инвариант #5).
4. **«Пользователи системы»** (`481b7c3`) — read-only вкладка кто-есть-кто (роль + активность + last seen)
   вместо сырых Telegram-имён. `GET /api/users/overview` (админ→все, владелец→свои контрибьюторы+себя).
5. **Поиск по фактам** (`3f946e8`) — `GET /api/search` (scope `client`|`all`), `matrix.search_facts`
   (кириллица регистронезависимо через SQLite-функцию `pylower`). `SearchBox` в шапке с переключателем
   «эта / все компании», подсветка, клик → `?cell=&fact=` открывает drawer и подсвечивает карточку.
6. **Собранные/переименованные карточки** (`3f946e8`…`7a0d1cf`) — итоговая семантика (после аудита прода:
   из 109 «собранных» 54 — переименование спикера, 55 — настоящие склейки):
   - **переименование спикера** (`attribute_fact`, note «переименован спикер») — это НЕ мерж:
     «спикер уточнён · ранее без имени: #id» (раскрытие «ранее (без имени) #id»).
   - **настоящая склейка**: «собрано из N карточек», где **N = только свёрнутые дубли** (сама
     видимая карточка НЕ в счёт, чипа «эта» нет). Исходники раскрываются inline (▸/▾).
   - провенанс: сентинелы (`merge`/`attribute`/`dev`) не автор → «собрано» / «собрал <имя>».
7. **Фавикон** (`424b387`) — `frontend/public/favicon.svg` (концентрические кольца, палитра матрицы).

## Открытые хвосты

- **Переселить факты по матрице v2** (за владельцем): per-client «Применить новую методологию» +
  регенерация досье. Важно для L6↔L8 (Philosophy→6.1, Market 8.2→6.2). Accumulator большой →
  прогон через Haiku платный/долгий — решение владельца. UI переклассификации это уже умеет.
- **gonka НЕ чистим намеренно** — кластер Хачуяна оставлен как живой тест на бдительность аналитика
  (см. память `ir-storyboard-gonka-khachuyan`).
- **ingest-пути** (LLM report / YouTube / audio) НЕ гейтят факты по роли (создаёт сразу active, не
  черновик). По желанию — провести через тот же review-гейт, что и ручной ввод.
- По желанию: global-режим переклассификации (все клиенты, супер-админ); дедуп/сканы на
  per-subsection+чанки, когда у клиента станет очень много фактов; стартовые шаблоны Brief.

## Деплой-нюансы (важно, не потерять)

1. Деплой только через workflow `deploy_ir_storyboard` (repo_path=/opt/ir-storyboard, branch=feat/v2,
   health_url=`http://172.17.0.1:80/`, projectId=ir-storyboard). Руками по ssh не деплоить, на сервере
   не коммитить. Реквизиты гейта: `source ~/.config/conductor-orchestrator/credentials`; паспорт —
   `PATCH http://localhost:5050/ui/api/projects/ir-storyboard/state` (X-Api-Key=ADMIN_API_KEY).
2. **Миграция схемы.** Startup-хук `_start_auth_poller` НЕ вызывает `init_schema` (частая ошибка в
   рассуждениях — не верить). Реально схему мигрирует **`get_conn` — на КАЖДОМ запросе**
   (`backend/main.py:114`), поэтому после деплоя новые колонки/таблицы добавляются при первом же
   обращении к /api. НО: если проверяешь прод-БД «голым» `db.connect()` без init_schema — увидишь
   старую схему и подумаешь, что сломано. Безопасно и сразу: после деплоя прогнать вручную
   `docker exec ir-storyboard-backend-1 python3 -c "from ir_storyboard import db; db.init_schema(db.connect('/app/data/matrix.db'))"`
   (идемпотентно). Проверять `facts_for_cell` — тоже через init'нутое соединение.
   TODO (по желанию): добавить `db.init_schema` в startup-хук, чтобы схема была готова на boot.
3. Прод-`/api` за Basic Auth — curl без логина отдаёт 401 (это норма, маршрут жив); health (`/`) открыт.

## Справка по «собранным» карточкам (модель данных)

Скрытый факт = `state='rejected'` + `merged_into=<id активной>`. Две природы, различаются по
`verification_note`:
- «переименован спикер → #N» — `attribute_fact` (иммутабельность: имя спикера нельзя вписать в
  текст на месте → новый факт + reject старого). НЕ мерж.
- «дубль — слит в #N» / «слит (с правкой текста) в #N» — `merge_facts` (default-fold / curated-синтез).
Инвариант #5: текст факта никогда не UPDATE-ится; любое «изменение» = новый факт, старый остаётся.
