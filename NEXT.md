# NEXT — где мы остановились

> Снимок оперативного состояния. Overwrite в конце сессии. Стабильное — в `CLAUDE.md`.

---

**Последнее обновление:** 2026-06-24
**Ветка:** `feat/v2` (запушена)
**HEAD:** `c36eec0 feat: Deliver — markdown-описание формата JSON + readme внутри выгрузки`
**Прод:** 216.57.108.107, перекатан 2026-06-24 ×4 (deploy_ir_storyboard зелёный, health 200).
Миграция `facts.title` применена на проде; заголовки сгенерированы по всем 17 клиентам
(2731/2732 фактов). Deliver-выгрузка проверена на проде (uzum 666 карточек).
Тесты: `pytest -m "not network"` → **278 passed** (сетевой `test_youtube_e2e` deselected).

## Сделано в этой сессии (2026-06-24)

- **Авто-заголовки карточек (#3 из батча правок).** `facts.title` (TEXT, '' = нет);
  `matrix.set_fact_title`; `verification.generate_fact_titles` — батч-LLM (_DEDUP_BATCH),
  заполняет ТОЛЬКО пустые (ручные правки не перетирает), stub-safe. backend:
  `FactOut.title`, `POST /facts/{id}/title`, `_compute_titles` +
  `POST /clients/{id}/generate-titles/start` (async job). Фронт: кнопка «Заголовки
  карточек» в шапке матрицы (Готово: N), заголовок bold +1pt над текстом, редактируемый.
- **Полировка карточки.** Собранная («joint») карточка несёт ТОЛЬКО ссылки на исходные
  (#N) — цитата/источник скрыты (провенанс живёт в исходных, которые всегда их показывают).
  Текст факта выровнен по ширине (justify).
- Проверено e2e в браузере (мок LLM, сид с joint-сценарием): заголовки bold/justify,
  редактирование заголовка персистит, joint-футер = только #2 #3, генерация 53 заголовков.
- Паспорт: веха «Авто-заголовки» (266) → done, добавлена веха полировки карточки.
- **Заголовки сгенерированы на проде** по всем 17 клиентам (2731/2732; 1 факт LLM
  пропустил — дозаполнится повторным кликом «Заголовки карточек»).
- **Deliver → «Выгрузка»: JSON-выгрузка содержания матрицы (тексты без ссылок).**
  `ir_storyboard/deliver.py` build_matrix_export; `GET /clients/{id}/matrix/export.json`.
  Три блока: export (описание + легенда цвет карточки/звезды), company_card
  (kind='company' из About, без ссылок), cards (matrix_no, title, text, card_color=флаг,
  star=blue клиент/purple эксперт). Только активные карточки. Фронт ExportView (превью
  по ячейкам + «Скачать JSON»/«Скопировать»). Тесты test_deliver_export.py. Проверено в
  браузере (локальный сид) + на проде через builder (uzum 666). Без миграций.
- **Описание формата JSON (markdown).** `deliver.describe_json_format` — md-разбор полей
  (export/company_card/cards, enum card_color/star); вложено в выгрузку как `readme`
  (самодокументируемый JSON). Эндпоинт `GET /clients/{id}/matrix/format.md`; в UI тоггл
  «Описание формата (md)» + «Скачать .md». Standalone-копия для заказчика лежит вне репо:
  `~/Downloads/matrix_export_format.md` (отдельный документ, по просьбе «просто описание»).
- **Раздел About — порядок (4 правки по фидбеку).** (1) Шапка = карточка
  идентичности: название·роль + блок «Официальные ссылки» (сайт/wiki/соцсети);
  убран дубль — секция «Сайты и каналы» удалена из UI (старые sites-факты →
  «Прочее»). (2) Человеческие ключи: `_SYSTEM` авто-наполнения просит русские
  ярлыки, фронт `humanizeKey()` чинит старые технические (product_category→
  Категория и т.п.). (3) Авто-поиск фаундеров: `company.build_founder_proposals`
  (веб+LLM, ссылки-профили обоснованы веб-источником) + `POST /clients/{id}/
  founders/discover/start`; в UI кнопка «Найти фаундеров» → модалка проверки →
  createEntity с links. (4) Вёрстка: max-w-5xl + `md:grid-cols-2`. Тест
  test_founder_discovery. Без миграций. Проверено в браузере + прод перекатан.
  **Доработка по реальным данным (accumulator/Dave Waiser):** humanizeKey срезает
  шумовые суффиксы (founded_date_fintech→Основана), неизвестный технический ключ
  скрывает; официальные ссылки в шапке собираются из entity.links + фактов секции
  «sites»; founderCandidates() предлагает чипом фаундера, упомянутого в фактах
  («Dave Waiser (ex-Gett CEO)»); вёрстка фактов: узкий ярлык-колонка + значение
  во всю ширину. Прод перекатан, COMPLETED.
  **Ещё итерация:** чип сайта = читаемый домен + href из значения (а не source_url —
  был баг: вёл на LinkedIn); linkInfo() распознаёт LinkedIn/X/GitHub/Wikipedia/News.
  Фаундер: фото-аватар (фолбэк инициалы) + кликабельные проф/соц-профили + кнопка
  «найти профили» (company.build_founder_profiles, веб+LLM, грунтовка) → POST
  /clients/{id}/founders/profiles/start. Вёрстка → ОДНА ШИРОКАЯ КОЛОНКА (грид убран).
  Тест test_founder_profiles. Прод COMPLETED.
  **Открытый запрос:** мониторинг сайта компании (периодический обход главной/news,
  диффы → сигналы). Пока заложено место (ссылка News в шапке). Это отдельная фича —
  спроектировать (что отслеживаем, частота, куда сигналы).
- **Фикс матрицы (звезда/легенда).** `cell_summary` разбивает must-have на
  `n_must_client` (синяя ★) / `n_must_expert` (фиолетовая ★) по `must_have_by`;
  ячейка рисует звёзды раздельно (раньше всё синим). Легенда переписана: RU-режимы
  (покрыто/риск/смешанно/пробел/пусто) + ★ клиента / ★ эксперта. test_cell_must_split.
  Без миграций. Прод перекатан (deploy зелёный).

**Открытое / следующее:** чистка gonka триажем (267, операционное, НЕ трогать кластер
Хачуяна без явной просьбы — учебный кейс); тест-гигиена изоляции БД (268); предкластеризация
дедупа/скана спикеров при сотнях фактов (269). «Ещё одна задачка» от пользователя — ждём.
Баланс Anthropic пополнен (2026-06-23) — LLM-функции (ингест/аудит/гайд/авторазбор/дедуп/скан
спикеров) работают на проде. YouTube-транскрипция — OpenAI Whisper (свой баланс).

## Scorecard: кликабельные цифры → набор карточек — 2026-06-25

По просьбе экспертов. Цифры green/red/grey в scorecard кликабельны: клик по цифре
строки-ячейки → модалка с карточками этого флага этой ячейки; клик по верхнему
тоталу → все карточки флага по клиенту. Карточка: флаг-точка, ячейка·#id,
★ must-have, заголовок, текст, gap/риск-пояснение, ссылка. Факты — через cellFacts
(useQueries), без бэкенда и миграций. `ScorecardView.tsx`. Прод COMPLETED.

## Досье клиента (консолидированная осведомлённость) — 2026-06-25

Слой над матрицей: зона Map → вкладка «Досье». Exec-summary (3-4 предл.) + синтез по
8 слоям (2-3 предл.) — LLM, кэш в `dossier_summaries` (тон analyst; present — задел),
кнопка «Пересобрать досье» (async job). Кольца осведомлённости (концентрика:
заполнение=покрытие, цвет=здоровье) + метрики (фактов/покрытие/корроборация/риски).
По слою — синтез + полоска «сколько знаем» (факты/ячейки/источники/свежесть/сигналы).
`ir_storyboard/dossier.py`; GET /clients/{id}/dossier, POST /dossier/generate/start.
Прод-миграция (таблица dossier_summaries) применена; проверено на uzum (666 фактов,
покрытие 88%). Тексты по клиентам не генерил массово (billable, по кнопке). Тесты
test_dossier. **B (презентационный тон) — на будущее: tone='present' уже в схеме.**

## Предложения аналитиков (правки после деки) — 2026-06-22

- **№6 Три дополнения: экспорт must-have / правка текста при слиянии / атрибуция спикера.**
  (a) ВЫГРУЗКА must-have: `matrix.must_have_facts` + `GET /clients/{id}/facts/must-have/export`
  отдаёт нумерованный «N — текст» .txt; кнопка «Выгрузить must-have (★N)» в шапке матрицы.
  (b) СЛИЯНИЕ ДУБЛЕЙ С ПРАВКОЙ: дедуп-LLM предлагает `merged_text`; `matrix.merge_facts(..., merged_text)`
  при заданном тексте создаёт НОВЫЙ факт (иммутабельность), складывает источники, отклоняет все
  оригиналы; в FactAuditView — редактируемое поле + «слить с этим текстом / оставить keep».
  (c) АТРИБУЦИЯ СПИКЕРА: `verification.find_unattributed_facts` (LLM-скан «фаундер считает…» →
  перепись с [ИМЯ]; 1 фаундер→авто, >1→needs_choice, L1-L2→must_be_concrete);
  `matrix.attribute_fact` (новый факт + speaker_entity_id, reject оригинала);
  `POST /find-unattributed(/start)` + `POST /facts/{id}/attribute`; в FactAuditView — кнопка
  «Проверить спикеров» + секция с дропдауном фаундеров; YouTube-коммит получил
  `speaker_entity_id` + пикер «С кем интервью?» (при >1 фаундере). Без новых миграций.
  Проверено e2e в браузере (мок LLM). ✓ на проде.
- **№6b Доработки фидбека.** Экспорт: «N (1.2) — текст» (добавлена ячейка). Слияние: при 3+
  фактах чекбоксы выбора подмножества (неотмеченные остаются в матрице); поле текста
  предзаполнено LLM-предложением. Атрибуция (починка «нет фаундеров/верификатор недоступен» —
  всплыло на Accumulator, где фаундеры не заведены, а LLM на проде без баланса): детерминированный
  keyword-fallback `find_unattributed_facts` работает офлайн; в UI можно ВПИСАТЬ имя фаундера →
  карточка создаётся на лету (`AttributeIn.new_founder_name`). Проверено e2e. ✓ на проде.
- **№5 YouTube ingest: прогресс + коммит из History.** (a) При распознавании — статус-бар
  (спиннер + текущая стадия + таймер + индетерминантный бар): `_yt_job_run` прокидывает
  `progress_cb` через `run_youtube_preview` → `get_or_transcribe` → `transcribe_audio_chunks`,
  стадии («метаданные/скачиваем аудио/транскрибирование: чанк N из M/извлекаем факты») пишутся в
  `job["stage"]`; опрос 5s→2.5s. (b) Корень «после прогона нельзя закоммитить»: схема
  `ingest_audit.confirmed_at` = `NOT NULL DEFAULT CURRENT_TIMESTAMP` → выставлялась уже на превью,
  любой прогон выглядел committed, reopen из History всегда read-only. Без перестройки таблицы:
  новая nullable-колонка `committed_at` (идемпотентный ADD COLUMN; на проде применил init_schema
  вручную, т.к. API за Basic Auth и get_conn не вызывался автоматически) — NULL до коммита,
  ставится в `run_youtube_commit`; в ответах history/preview-by-id поле `confirmed_at` маппится из
  `committed_at` (фронт без изменений). Фронт: `readOnly = !!confirmed_at`, незакоммиченный reopen
  редактируем+коммитится. NB: старые прод-строки youtube-ингеста теперь committed_at=NULL →
  показываются pending и доступны для (идемпотентного) пере-коммита. Проверено e2e в браузере
  (мок внешних границ). ✓ на проде.

- **№1 Research: двухшаговый поиск.** Сначала «Подобрать запросы» (генерация без поиска) → аналитик
  правит запросы в редактируемом поле (по строке) → «Искать». Бэкенд: `POST /research/queries`
  (только генерация) + `/research` принимает `{queries}` (правленые) с фолбэком на авто-сборку.
  Вкладка Build → Research. ✓ на проде.
- **№2 Привязка факта к фаундеру.** История с двумя Либерманами: факты не были прибиты к человеку.
  Теперь `facts.speaker_entity_id` (FK entities), на факте в дровере пикер «🗣 кто говорит?»
  (список фаундеров), а в карточке компании «About» — секция «Фаундеры» (список/добавить/удалить),
  которая питает выбор. `POST /facts/{id}/speaker`. Колонку на проде создавал заранее. ✓ на проде.
- **№3 LLM-report: 3 режима ввода.** Вкладка Ingest LLM report теперь: «Файл» (как было) /
  «Промпт + ответ» (генерим deep-research промпт под клиента+агента из шаблонов → Copy → вставляешь
  ответ → парсится тем же пайплайном; `research_prompt.py`, `GET .../prompt` + `POST .../preview-text`) /
  «Произвольный PDF» (любой PDF, в т.ч. картиночный → Claude vision читает напрямую → факты
  ложатся в матрицу L2–L8 → аналитик отмечает → коммит как archival, источник = файл;
  `llm.extract_facts_from_pdf`, `POST .../other-pdf/preview` + `.../other-pdf/commit`). ✓ на проде.
- **№4 Must-have факты от клиента (синий) + вес в Deliver.** Факты, присланные клиентом лично —
  ключевые. Реализовано оверлеем `facts.must_have` (boolean), НЕ 4-м значением `flag` (избегаем
  перестройки таблицы из-за `CHECK(flag IN green/red/grey)`; идемпотентный ALTER в init_schema).
  Рендер синим: `FlagDot mustHave`, tailwind `flag.blue`, в дровере синяя точка + ★-префикс +
  ★-тоггл (`POST /facts/{id}/must-have`, `matrix.set_fact_must_have`), в ячейке матрицы синий
  счётчик `★N` (`cell_summary.n_must`). Новая Build-вкладка **«От клиента»** (`IngestClientFacts`):
  вставка фактов → `POST /clients/{id}/ingest/client-facts` (канал offline_interview — провенанс
  по названию источника, разрешает L1-L3; `must_have=True`). Deliver: `brief.py` выносит must-have
  отдельным блоком «★ Must-have от клиента (обязательно отрази)» в начало + ★-префикс + правило в
  BRIEF_RULES + поле в render_json. Тест `tests/test_must_have_api.py` (тоггл / ingest / n_must).
  Проверено e2e в браузере (seed-бэкенд): вставка факта в 3.3 → синий в дровере и в матрице,
  brief содержит блок must-have. Колонка `must_have` (DEFAULT 0) применяется init_schema при
  старте контейнера — пред-создание не требовалось. ✓ на проде.
- **№4b Авторазбор файла от клиента.** Вкладка «От клиента» получила переключатель «Вписать
  вручную / Загрузить файл». Файл-режим: клиент прислал PDF (в т.ч. скан) → `extract_facts_from_pdf`
  (Claude vision) разбирает автоматом → превью кандидатов (правка ячейки/флага, чекбоксы) →
  «Внести N синими в матрицу» (коммит через существующий `/ingest/client-facts`, must_have=True).
  Корень «pdf не разобрался»: `POST .../client-facts/preview` открывает ВСЕ слои L1–L8 (в отличие
  от other-pdf, где `L>=2` молча отбрасывал личные факты про фаундера — L1). `api.clientFactsPreview`.
  Тесты: preview допускает L1 + коммит must_have; non-PDF → 400. Проверено e2e в браузере
  (seed + мок-извлечение): 4 факта по L1.1/1.2/4.1/3.3 → все синие в матрице. ✓ на проде.
- **№4c КОРЕНЬ «не распознаёт».** Баг в `llm._try_init_anthropic`: `_client =
  anthropic.Anthropic(...)` создавался ЛОКАЛЬНО — `_client` не было в `global`-декларации.
  Классификация/generate работают (читают _client из замыкания), а `extract_facts_from_pdf`
  (module-level) берёт `globals().get("_client")` → None → тихо `[]` даже при валидном ключе.
  Это ломало ОБА потока — «Произвольный PDF» (№3) и авторазбор «От клиента» (№4b). Фикс:
  `_client` в `global` + module-level `_client = None`. Подтверждено на проде: vision реально
  читает PDF (протокол распознан дословно). Тест-гард `test_pdf_vision_client.py` (3) ловит
  closure-local регрессию. + отвергнутый файл (повреждён/зашифрован) теперь даёт 422 с понятным
  текстом (`PdfRejectedError`), а не тихий 0; фронт `uploadError()` показывает `detail`.
  ⚠️ **БЛОКЕР (операционный, не код):** прод-аккаунт Anthropic исчерпал баланс —
  «Your credit balance is too low». Пока не пополнить, ВСЕ LLM-функции на проде (ingest,
  аудит, гайд, research, авторазбор) возвращают пусто. Нужно пополнить кредиты в Anthropic
  Console (billing — действие пользователя).

**Карточка компании «About» (2026-06-20):** компания — отдельная первоклассная сущность, не
строка в якоре верификации. Вкладка **Map → «About»** (первой, перед Matrix). Структурный
бизнес-профиль без нарратива: факты со ссылками сгруппированы по фиксированным секциям
(Профиль / Сайты / Финансирование / История / Продукт / Метрики). Наполняется руками (ingest —
следующий шаг). Хранилище: `entity_facts.section` (идемпотентная миграция; на проде колонку
создавал заранее перед деплоем). UI: `CompanyAbout.tsx` (секции, факт = значение+дата+источник →
verified, add/del, ссылки в шапке, авто-создание карточки). App: вкладка about первой в Map;
имя+сектор компании в верхней панели (зазор Deliver↔Methodology); сайдбар авто-сворачивается
при выборе клиента. Проверено e2e в браузере. У gonka.AI карточка «Gonka.ai» уже есть (из аудита).

**Авто-наполнение карточки «About» (2026-06-20):** `ir_storyboard/company.py`
`build_about_proposals` — два источника в один LLM-проход: (1) уже собранные проверенные факты
клиента + их URL, (2) веб-поиск (Tavily) по названию. Дисциплина «голые факты, 100% уверенность»:
модель может отдать только факты, чей `source_url` ДОСЛОВНО есть в предоставленных источниках —
механический фильтр (whitelist из allowed-URL), не только промпт; всё, что мимо — отброшено.
Ничего не пишется без подтверждения аналитика. API: `POST /company/autofill/start` (фоновый job
через `_start_llm_job`) + `/company/autofill/commit` (пишет принятые, создаёт карточку при
необходимости). UI: «Авто-наполнить» → прогресс → модалка-staging по секциям с чекбоксами,
бейджем источника (матрица/веб) и ссылкой → коммит. Ручной ввод работает параллельно.
Баг по пути: `entity_facts.as_of` имеет DATE-аффинность — SQLite приводит голый год «2024» к int
→ 500 на /entities; чиним приведением к str при чтении. Проверено e2e в браузере (канва).

**Завершение карточки «About» (2026-06-20):** добавлены последние два куска.
- Третий источник авто-наполнения — «Из документа»: аналитик вставляет deep-research отчёт/статью
  + URL источника; факты извлекаются с привязкой к этому URL (origin=doc), веб-поиск выключен.
  Та же грунт-дисциплина + staging→коммит. `build_about_proposals(pasted, pasted_url,
  pasted_title, use_web)`; start-эндпоинт принимает опциональный body; `runJob` шлёт body.
- Инлайн-правка заголовка карточки: ✎ → имя / роль / note (`patchEntity`). Бейдж источника
  различает матрица / веб / документ.
Проверено e2e в браузере: вставка → извлечение → коммит (факт со ссылкой на документ); правка
роли/note сохраняется.

**Открыто (масштаб дедупа/сканов — на будущее):** дедуп теперь идёт по подсекциям с чанками
по `_DEDUP_BATCH`=30 фактов (интерим-предохранитель от обрезки запроса). Минус блайнд-чанкинга:
пара-дубль, разнесённая по разным батчам одной подсекции, в этот прогон не находится (ловится
повторным прогоном после слияний). Полноценное решение: дешёвая ПРЕДкластеризация фактов внутри
подсекции (Jaccard/эмбеддинги) → в один батч попадают похожие, LLM добивает точную кластеризацию.
То же касается скана спикеров (`find_unattributed_facts`) при сотнях фактов — пока шлёт все одним
запросом, тоже стоит перевести на per-subsection + чанки. TODO когда у клиента станет очень много
фактов в одной подсекции.

**Открыто (операционное, не билд):** почистить gonka триажем в «Проверке фактов» — снять кластер
Хачуяна (мутирует реальные данные клиента, делать с подтверждением). Хвосты реестра: тест-гигиена.

**Редактируемый якорь идентичности (2026-06-20):** `IdentityAnchor` был read-only, хотя бэкенд
CRUD по `entities`/`entity_facts` полностью готов. Теперь карточка (компания/фаундер/двойник) —
курируемый источник правды: добавить факт (ключ/значение + ссылка-источник + название; ссылка →
verified, без ссылки → предупреждение), добавить/убрать ссылки первоисточников (Wiki/X/LinkedIn)
через PATCH links (полная замена), удалить факт, создать карточку вручную. Всё через
существующие эндпоинты (`createEntity`/`addEntityFact`/`deleteEntityFact`/`patchEntity`).
Компоненты `EntityCard`/`AddFactForm`/`AddLinkForm`/`AddEntityForm` в `FactAuditView.tsx`.
Проверено e2e в браузере-превью (seed-бэкенд): add-fact/add-link/delete-fact доходят до БД.
Не сделано (по желанию): карандаш для правки заголовка карточки (имя/роль/note).

**Единый LLM-JSON примитив (2026-06-20):** «бились каждый раз», потому что 6 мест в `llm.py` +
verification + interview парсили JSON одинаково слабо (срез ``` только если ответ С НЕЁ
начинается → ломается на прозе-преамбуле). Сведено в два примитива в `llm.py`:
`extract_json(raw, repair=False)` (весь текст → fenced-блок где угодно → широкий `{...}`/`[...]`
спан → опц. ремонт обрезанного по max_tokens; отдаёт dict|list|None) и `generate_json(...)`
(generate + extract_json + ретрай на пустой/битый). Все 6 экстракторов (report/full-doc/
transcript/youtube-summary/research) переведены; `verification._parse_json/_generate_json` и
interview — тонкие обёртки над примитивом. Это ЕДИНСТВЕННОЕ место, где теперь чинить парсинг
LLM-JSON. Проверено на проде: audit 28с/12 склеек, guide 120с/5 дуг — оба через примитив.

**Асинхронные LLM-задачи (2026-06-20) — фикс «Failed to fetch» на гайде:** `build_guide` идёт
50–116с; долгий синхронный запрос рвёт NAT/файрвол по простою соединения (~60с без байтов →
браузер видит «Failed to fetch»). Аудит (30–45с) проскакивал, гайд — нет. Лечение — те же
фоновые job-и, что у YouTube-ингеста:
- backend: generic `_llm_jobs` стор + `_start_llm_job`/`_llm_job_run` (своя БД-коннекция на
  поток). `POST .../audit|find-duplicates|interview-guide/start` → `{job_id}`; `GET /jobs/{id}`
  → `{status,result,error}`. Синхронные эндпоинты сохранены (логика вынесена в
  `_compute_audit/_compute_dups/_compute_guide`, общие для обоих путей).
- frontend: `api.runJob(startPath)` стартует и поллит `/jobs/{id}` раз в 2.5с; runAudit/
  findDuplicates/interviewGuide — через него. Формы результата не изменились. Прогресс-бар
  тикает реальные секунды; каждый поллинг — мгновенный запрос, висящих соединений нет.
- Проверено на проде: audit job 30с (16 склеек), guide job 116с (7 дуг, 13 вопросов) — оба done.

**Ретрай на транзиент (2026-06-19):** даже с надёжным парсером сильная модель изредка отдаёт
пустой/битый ответ (перегруз) → раньше это всплывало как «Верификатор недоступен (нет ключа
LLM)» (вводит в заблуждение — ключ есть). `verification._generate_json` (generate + tolerant
parse, attempts=2) — общий путь для audit/verify/gate/dedup; гайд интервью переиспользует его
(свой слабый `_parse` удалён). UI-сообщения переписаны: «модель не ответила — попробуй ещё раз».
На проде после фикса: 3/3 прогона аудита gonkaai → available, 11–15 склеек.

**Фикс «кнопка проверки мёртвая» (2026-06-19) — настоящий корень:** sonnet на аудите 157
фактов отдаёт ПРОЗУ-преамбулу + JSON в ```-ограждении. Старый `_parse_json` срезал ``` только
если ответ С НЕЁ начинался → `json.loads` падал на всём тексте → `audit_client` возвращал
`available:False` без находок → в UI тишина. Это же било по дедупу/воротам/верификации (общий
парсер). Починка:
- `verification._parse_json` — тянет JSON из прозы/ограждений где угодно (весь текст → fenced-блок
  → самый широкий `{...}`). +юнит-тесты; в промпт аудита добавлен запрет преамбулы. На живой
  gonkaai: было 0 находок → стало 13–17 склеек (Product Science/SocialDataHub/Tazeros как двойники).
- UI: `RunProgress` (бар + счётчик секунд во время ~30–60с вызова LLM) в «Проверке фактов» и в
  гайде; явные блоки ошибок вместо молчания (`isError`).
- Персист `audit/dups/guide` в localStorage (`fact-audit-<id>`, `interview-guide-<id>`): запись
  ВНУТРИ `mutationFn` (результат приземляется, даже если ушёл с вкладки во время счёта), чтение —
  в инициализаторе `useState` на remount. Это проверенный паттерн Ingest-вью; переживает и
  переключение, и перезагрузку. (Предыдущая попытка через react-query-кэш «не сработала» не из-за
  кэша, а из-за `available:False` — нечего было персистить.)
- Остаётся тот же localStorage-паттерн недокручен в ResearchView/BriefComposer (превью в локальном
  state) — транзиентный кейс, не трогал.

## Что сделано за сессию: верификация фактологии — Фаза 1

Проблема: research/deep-research факты галлюцинируют и склеивают сущности (на gonka
биографию Хачуяна слепило с Либерманами в одного «фаундера»). YouTube/аудио безопасны
(цитаты+таймкоды). Построен контур доверия.

- **Схема** (`db.init_schema`): facts += `verification` (verdict), `verification_note/_sources/_at`,
  `entity`, `state` (active|rejected). Новые `entities` + `entity_facts` — якорь идентичности
  (компания/фаундеры/двойники) вне нарративной матрицы, source-linked.
- **Движок** `ir_storyboard/verification.py`: `audit_client` (скептический аудит сущностей,
  per-fact вердикт + предложенный canonical), `verify_claims` (Tavily + grounded-вердикт).
  Транскриптные факты не проверяются. Stub-safe.
- **API** (`backend/main.py`): `POST /clients/{id}/audit` (гоняет аудит, проставляет вердикты,
  бутстрапит черновик якоря), `verify-claims`, `/facts/{id}/verification|reject|restore`,
  CRUD `/entities`. Rejected исключён из агрегатов/циклов/scorecard/punch; в дровере ячейки
  показан зачёркнутым + «вернуть».
- **UI**: вкладка Health → «Проверка фактов» (`FactAuditView`): якорь идентичности (карточки,
  подтверждение, ссылки) + триаж (снять/оставить, массово по сущности, прыжок в ячейку).
  Бейджи доверия + чип сущности + снять/вернуть в `CellDrawer`.
- **Тесты**: `test_verification.py` (5) + `test_verification_api.py` (4), офлайн.
- **Валидировано на боевой gonka** через задеплоенный модуль: available=True, 23 факта
  флагнуто из 157 archival, canonical company=`Gonka.ai`. Прототипом ранее подтверждено,
  что веб-проверка чисто разводит Хачуян↔Либерман.

**Фаза 2 — ворота на ингесте:**
- `verification.verify_candidates(candidates, anchor)` — скептический фильтр ПЕРЕД коммитом:
  флагает кандидатов, относящихся к двойнику / противоречащих якорю / похожих на выдумку.
  Нет якоря или stub → пропускает всё (degrade-open; первый батч ловит переаудит).
- `commit_llm_report` верифицирует извлечённые факты до записи; suspect/refuted коммитятся,
  но в карантин `state='review'` (вне матрицы), пока человек не промоутит. Возвращает `held_facts`.
- API: `GET /clients/{id}/review-queue`, `POST /facts/{id}/promote`. UI: секция «На ревью с
  ингеста» в «Проверке фактов» (в матрицу / снять); в результате ингеста «N придержано воротами».

**Фаза 3 — дедуп/слияние:**
- Новая таблица `fact_sources` (мультиисточник для слитого факта).
- `verification.find_duplicate_groups()` — LLM-кластеризация околодублей по подсекции →
  предложения слияния. `matrix.merge_facts(keep, ids)` — сворачивает источники дублей в
  корроборацию keep'а, дубли мягко-rejected. `n_sources` = 1 + слитые источники (сигнал доверия).
- API: `POST /clients/{id}/find-duplicates`, `POST /facts/merge`. UI: секция «Дубли»
  (найти → группы → слить в один) в «Проверке фактов»; бейдж «N независимых источника» в ячейке.

**Грунтованный гайд интервью (заменил статические шаблоны):**
- `ir_storyboard/interview.py` `build_guide()` — одна проходка по ПРОВЕРЕННЫМ фактам
  (state=active, не suspect/refuted) + якорь → структурный JSON {dossier, diagnosis
  {covered,gaps,priorities}, arcs[questions{question,targets,know,close,followups}]}.
- API `POST /clients/{id}/interview-guide`. UI: `InterviewView` переписан (Сгенерировать
  гайд → досье + диагноз + дуги; вопросы грунтованы, targets прыгают в ячейки, follow-ups).
- Валидировано на gonka: 8 дуг, грунтованные вопросы. Замкнутая петля: почистил матрицу
  через «Проверку фактов» → гайд авто-улучшается.

**Дисциплина грунтования гайда:** факты в промпт идут с `id`; модель обязана указать `grounds`
(id фактов-опор) на каждый вопрос и НЕ называть числа/имена/даты, которых нет в процитированных
фактах (вместо этого — спросить у фаундера). `grounds` резолвятся в тексты, неизвестные id
отбрасываются. UI: под вопросом «основано на N факт(ах)» (раскрывашка); без опоры — «открытый
вопрос». Валидация на gonka: 15/15 вопросов с grounds, выдуманное «N приложений» ушло.

## Деплой-нюансы

1. **health_url в `deploy_ir_storyboard` = публичный IP** `http://216.57.108.107/api/health`,
   НЕ localhost (HTTP-таск исполняет JVM Conductor'а в своём контейнере). С этим фиксом Фаза 2
   задеплоилась полностью зелёной (health + mark_deploy сработали).
2. **Миграция при деплое не применяется автоматом.** Прод backend — `uvicorn` БЕЗ `--reload`
   (CMD в `backend/Dockerfile`), новый код встаёт при рестарте контейнера, и тогда startup-хук
   с init_schema мигрирует. Но если новый код ЧИТАЕТ новую таблицу (как Фаза 3: facts_for_cell
   ссылается на `fact_sources`), есть окно поломки между git_pull и применением миграции.
   Приём (Фаза 3): **заранее создать таблицу на проде** (`CREATE TABLE IF NOT EXISTS`, старый код
   игнорирует) ДО деплоя — окна нет. После деплоя — прогнать `db.init_schema` (идемпотентно).
   TODO: добавить шаг миграции/restart в deploy-workflow, чтобы не вручную.
3. `audit_client` разок вернул `available=False` (транзиент LLM); повтор — ок. TODO: ретрай.

## Презентация для аналитиков (2026-06-21)

`docs/analyst-deck/` — 10 слайдов (pptxgenjs, RU): большая идея (матрица + доверие), навигация,
матрица, каналы наполнения, контур доверия, практика «поймай Хачуяна» на gonka, карточка About,
гайд интервью, рабочий цикл + дисциплина. Файл `IR-Storyboard-гайд-аналитика.pptx`. Пересборка —
`node build.js` (README рядом). Визуально отQA'ено (10/10 чисто, LibreOffice-рендер).

## Дальше

Карточка About (ручной ввод + авто-наполнение 3 источника + правка заголовка) и презентация —
готовы и на проде/в репо. Открыто (по желанию):
- gonka НЕ чистим намеренно — кластер Хачуяна оставлен как живой тест на бдительность аналитика.
- Хвосты реестра: стартовые шаблоны Brief; тест-гигиена (изоляция тестовой БД).
- TODO деплой: добавить шаг миграции/restart в `deploy_ir_storyboard` (см. деплой-нюансы).
